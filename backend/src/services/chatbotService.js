// Placeholder chatbot service - integrate with selected AI/chatbot API

const askBot = async (message) => {
  // Implement real API calls here
  return { reply: `Echo: ${message}` };
};

module.exports = { askBot };
